use rustc_serialize::json::Json;


pub fn json_to_gron(json: &Json) {
    // let mut stack = Vec::with_capacity(8);
    // let mut namebuf = String::with_capacity(100);
    // stack.push((json, 0usize));
    // while stack.len() > 0 {
    //     let
    //     match json {
    //         I64(value) =>
    //         U64(u64),
    //         F64(f64),
    //         String(String),
    //         Boolean(bool),
    //         Array(Array),
    //         Object(Object),
    //         Null,
    // }
}

